## 1. Dependency Updates

- [x] 1.1 Add `protobuf` and `sentencepiece` to `backend/requirements.txt`.
- [x] 1.2 Pin `bcrypt==3.2.2` in `backend/requirements.txt`.

## 2. Backend Logic

- [x] 2.1 Re-enable admin user initialization in `backend/main.py`.

## 3. Verification

- [ ] 3.1 Rebuild Docker container: `docker-compose up --build`.
- [ ] 3.2 Confirm "Model loaded." appears in logs without errors.
