## MODIFIED Requirements

### Requirement: Central File Management Dashboard
The system SHALL provide a dedicated dashboard page (accessible via `/dashboard`) that lists the history and status of processed files.

#### Scenario: Access History via Sidebar
- **WHEN** a user clicks "Dashboard" in the sidebar
- **THEN** the system SHALL navigate to `/dashboard` and display the historical job list.
